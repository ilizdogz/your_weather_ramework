# Uncomment the next line to define a global platform for your project
platform :ios, '11.2'
use_frameworks!

target 'WeatherFramework' do
    pod 'SwiftyJSON', '~> 4.0.0'
    pod 'Timepiece', '~> 1.3.1'
end
